dependencies:
    override:
        - npm install
    cache_directories:
        - node_modules

general:
    artifacts:
        - "semiscreen.crx"

test:
    post:
        - echo "$PRIVATE_PACKER_KEY" > extension.pem
        - bash crxmake.sh src/ extension.pem && mv src.crx semiscreen.crx
